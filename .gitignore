/target
/dist
# Cargo/target caches for the `ci` compose service; host dirs so a runner can cache them.
/.ci-cache
**/*.rs.bk
/.cairn
/fixtures
*.sqlite
*.sqlite-wal
*.sqlite-shm

# spike outputs (SCIP indexes are tens of MB)
/spike/out/*.scip
/spike/out/*.log
